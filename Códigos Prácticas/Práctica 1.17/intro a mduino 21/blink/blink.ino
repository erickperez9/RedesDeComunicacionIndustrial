void setup() {
  // put your setup code here, to run once:

}

void loop() {
  // put your main code here, to run repeatedly:
  digitalWrite(Q0_0, HIGH);//Writing a HIGH on a Digital Output
  delay(500);
  digitalWrite(Q0_0, LOW);//Writing a HIGH on a Digital Output
  delay(500);
  digitalWrite(Q0_2, HIGH);//Writing a HIGH on a Digital Output
  delay(500);
  digitalWrite(Q0_2, LOW);//Writing a HIGH on a Digital Output
  delay(500);
  digitalWrite(Q0_4, HIGH);//Writing a HIGH on a Digital Output
  delay(500);
  digitalWrite(Q0_4, LOW);//Writing a HIGH on a Digital Output
  delay(500);
  digitalWrite(Q0_6, HIGH);//Writing a HIGH on a Digital Output
  delay(500);
  digitalWrite(Q0_6, LOW);//Writing a HIGH on a Digital Output
  delay(500);
}
